# AdEarn Production App

## Setup

1. Copy `.env.example` → `.env` and fill values.
2. Install dependencies:
   flutter pub get
   npm install (functions)
3. Firebase deploy:
   firebase deploy --only functions,hosting,firestore
4. Run Flutter:
   flutter run
